# ─────────────────────────────────────────────
# AWS CDK (todos los lenguajes)
# ─────────────────────────────────────────────
cdk.out/
*.d.ts
.cdk.staging/

# ─────────────────────────────────────────────
# Node.js / TypeScript
# ─────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-error.log*
*.js.map
# Compilados de TS (descomenta si aplica)
# *.js

# ─────────────────────────────────────────────
# Python
# ─────────────────────────────────────────────
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/

# ─────────────────────────────────────────────
# Java / Maven
# ─────────────────────────────────────────────
target/
*.class
.classpath
.project
.settings/

# ─────────────────────────────────────────────
# Go
# ─────────────────────────────────────────────
# bin/  ← COMENTADO A PROPÓSITO: ignoraba 'bin/' a secas y excluía cdk/bin/cdk.ts
#          (el entrypoint de CDK). El repo quedaba sin poder ejecutar `cdk synth`.
#          Si algún día compilas binarios de Go, usa '/bin/' (solo raíz) en vez de 'bin/'.
*.exe

# ─────────────────────────────────────────────
# C# / .NET
# ─────────────────────────────────────────────
obj/
*.user

# ─────────────────────────────────────────────
# Credenciales y entorno (¡nunca commitear!)
# ─────────────────────────────────────────────
.env
.env.*
*.pem
# Excepción (Boss Fight): la llave PÚBLICA de CloudFront sí debe subirse al repo.
# La privada (cloudfront-private.pem) queda igual bloqueada por la regla de arriba.
!cdk/keys/cloudfront-public.pem
aws-exports.js

# ─────────────────────────────────────────────
# Editores / SO
# ─────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp
